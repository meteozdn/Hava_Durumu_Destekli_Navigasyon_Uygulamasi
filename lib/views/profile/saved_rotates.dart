import 'package:flutter/material.dart';

class SavedRotatesView extends StatelessWidget {
  const SavedRotatesView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Kayıtlı Rotalar"),
      ),
    );
  }
}
